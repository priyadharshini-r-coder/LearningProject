<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.learningproject">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <permission
        android:name="com.example.learningproject.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.learningproject.permission.MAPS_RECEIVE" />

    <application
        android:name=".uberclone.di.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        tools:replace="android:icon"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearningProject">
        <activity
            android:name=".uberclone.SplashActivity"
            android:exported="false" />
        <activity
            android:name=".uberclone.activities.DialogActivity"
            android:exported="false" />
        <activity
            android:name=".uberclone.coroutines.TwoLongRunningTask"
            android:exported="false" />
        <activity
            android:name=".uberclone.coroutines.longrunningtask.LongRunningTaskActivity"
            android:exported="false" />
        <activity
            android:name=".uberclone.coroutines.roomdb.RoomDbActivity"
            android:exported="false" />
        <activity
            android:name=".uberclone.coroutines.singlenetwork.SingleNetworkActivity"
            android:exported="false" />
        <activity
            android:name=".uberclone.coroutines.seriesnetworkcall.SeriesNetworkCallsActivity"
            android:exported="false" />
        <activity
            android:name=".uberclone.coroutines.parallelnetworkcall.ParallelNetworkCallsActivity"
            android:exported="false" />
        <activity
            android:name=".uberclone.activities.SettingsActivity"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".uberclone.activities.CustomerMapsActivity"
            android:exported="false"
            android:label="@string/title_activity_customer_maps" />
        <activity
            android:name=".uberclone.activities.DriverMapsActivity"
            android:exported="false"
            android:label="@string/title_activity_driver_maps" />
        <activity
            android:name=".uberclone.activities.DriverLoginRegister"
            android:exported="false" />
        <activity
            android:name=".uberclone.activities.CustomerLoginRegister"
            android:exported="false" />
        <activity
            android:name=".uberclone.activities.WelcomeActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".uberclone.di.DependencyInjection"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".uberclone.service.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>